<div class="row">
  <div class="col-lg-12">
    <h4>Your playlists:</h4>
    <ul>
      <% @playlists.each do |playlist| %>
        <li>
          <a href="/playlists/<%= playlist[:id] %>"><%= playlist[:name] %></a>
        </li>
      <% end %>
    </ul>
  </div>
</div>
