<div class="row">
  <div class="col-lg-12">
    <ul>
      <% @tracks.each do |track| %>
        <li>
          <%= track.name %>
          <!-- <%= track.artists.map(&:name).join(' ') %> -
          <%= track.name %> -->
        </li>
      <% end %>
    </ul>
    <code>
    <%= @playlist.tracks %>
    </code>
  </div>
</div>
